# server {
#     listen 80;
#     server_name login.localtest.me;

#     location / {
#         proxy_pass                              http://oauth2-proxy.localtest.me:4180;
#         proxy_set_header    Host                $http_host;
#         proxy_set_header    X-Real-IP           $remote_addr;
#         proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
#         proxy_set_header    X-Request-ID        $request_id;
#     }
# }

server {
    listen 80;
    server_name *.localtest.me;

    auth_request /oauth2/auth;

    error_page 401 =403 http://oauth2-proxy.localtest.me:4180/oauth2/sign_in?rd=$scheme://$host$request_uri;

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    location /oauth2/auth {
        internal;

        proxy_pass                              http://oauth2-proxy:4180;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }
}

server {
    listen 80;
    server_name landing.localtest.me;

    location / {
        root /usr/share/nginx/html;
        index landing.html;
        try_files $uri $uri/ =404;
    }
}

server {
    listen 80;
    server_name authentication-server.localtest.me;

    auth_request /oauth2/auth;

    location /oauth2/auth {
        internal;

        proxy_pass                              http://oauth2-proxy:4180;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }

    error_page 401 =403 http://oauth2-proxy.localtest.me:4180/oauth2/sign_in?rd=$scheme://$host$request_uri;

    location ~ ^/(permission) {
        proxy_pass                              http://authentication-server:8080;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }
}

server {
    listen 80;
    server_name hi-server.localtest.me;

    auth_request /oauth2/auth;

    location /oauth2/auth {
        internal;

        proxy_pass                              http://oauth2-proxy:4180;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }

    error_page 401 =403 http://oauth2-proxy.localtest.me:4180/oauth2/sign_in?rd=$scheme://$host$request_uri;

    location ~ ^/(hi) {
        proxy_pass                              http://hi-server:8080;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }
}

server {
    listen 80;
    server_name hello-server.localtest.me;

    auth_request /oauth2/auth;

    location /oauth2/auth {
        internal;

        proxy_pass                              http://oauth2-proxy:4180;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }

    error_page 401 =403 http://oauth2-proxy.localtest.me:4180/oauth2/sign_in?rd=$scheme://$host$request_uri;

    location ~ ^/(hello) {
        proxy_pass                              http://hello-server:8080;
        proxy_set_header    Host                $http_host;
        proxy_set_header    X-Real-IP           $remote_addr;
        proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header    X-Request-ID        $request_id;
    }
}